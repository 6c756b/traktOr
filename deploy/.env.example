FTP_HOST=ftp.example.com
FTP_USER=username
FTP_PASS=password
FTP_REMOTE_DIR=/httpdocs

# URL path under which the app will be reachable (e.g. https://domain.tld/traktor/).
# Leave empty if the app runs at the domain root.
BASE_PATH=
